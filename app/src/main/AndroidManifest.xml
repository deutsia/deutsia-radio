<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.opensource.i2pradio">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <application
        android:name=".I2PRadioApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="deutsia radio"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.I2PRadio"
        android:usesCleartextTraffic="true">

        <!--
            Android Auto descriptor. This meta-data only becomes active when
            the DeutsiaMediaLibraryService component below is enabled. That
            component is disabled by default; the user opts in via Settings,
            which flips the component state at runtime. Until then, Android
            Auto cannot see or bind to the app.
        -->
        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".AuthenticationActivity"
            android:exported="false"
            android:theme="@style/Theme.I2PRadio"
            android:screenOrientation="portrait"
            android:launchMode="singleTop" />

        <service
            android:name=".RadioService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <service
            android:name=".tor.TorService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!--
            Opt-in Android Auto media library service.

            Declared with android:enabled="false" so that, by default,
            Android Auto cannot bind to it and the app does not appear in
            the car's media source list. AndroidAutoComponentManager flips
            this component on via PackageManager.setComponentEnabledSetting
            when the user enables Android Auto from Settings, and flips it
            back off when the user disables it.
        -->
        <service
            android:name=".auto.DeutsiaMediaLibraryService"
            android:enabled="false"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaLibraryService" />
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>

    </application>

</manifest>
