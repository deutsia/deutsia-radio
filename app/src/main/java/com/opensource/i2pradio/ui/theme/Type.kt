package com.opensource.i2pradio.ui.theme

import androidx.compose.material3.Typography

/**
 * Default Material 3 Typography.
 *
 * The existing XML layouts rely on Material 3's default type scale (no custom
 * font family is applied via themes.xml). Keeping this as the default scale
 * preserves current text sizing/weights. Customize here once we've finished
 * migrating screens and have a clearer picture of what overrides we want.
 */
val DeutsiaTypography = Typography()
